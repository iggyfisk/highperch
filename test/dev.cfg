# These can all be absolute paths anywhere on the filesystem,
# but must be write accessible by the web app, also both
# REPLAY_PATH and PEEP_PATH must be web accessible as /static/replays
# and /static/images/peep
DB_PATH = '../'
TEMP_PATH = '../temp'
REPLAY_PATH = 'static/replays'
REPLAY_DATA_PATH = '../replaydata'
PEEP_PATH = 'static/images/peep'
# https://flask.palletsprojects.com/en/1.1.x/config/#builtin-configuration-values
DEBUG = True
ENV = 'development'